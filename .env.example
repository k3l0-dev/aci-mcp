# ── APIC credentials ──────────────────────────────────────────────────────────
# No APIC of your own? The free Cisco DevNet Always-On ACI sandbox publishes its
# hostname and credentials at https://devnetsandbox.cisco.com — take them from
# there, not from a copy in this file, since Cisco rotates them.
APIC_HOST=https://your-apic.example.com
APIC_USER=admin
APIC_PASSWORD=your_password

# Set to "true" to enforce TLS certificate verification (recommended in production)
APIC_VERIFY_SSL=false

# ── MCP server ────────────────────────────────────────────────────────────────
# Interface to bind. Loopback by default, so a fresh install is not reachable
# from the network. A routable value (0.0.0.0, or a specific address) requires
# MCP_API_KEYS below — the server refuses to start otherwise, because it holds
# your APIC credentials.
MCP_HOST=127.0.0.1
MCP_PORT=8000

# Bearer tokens for API key authentication (comma-separated).
# Leave unset to disable auth (local development only).
# Generate tokens with: python -c "import secrets; print(secrets.token_urlsafe(32))"
MCP_API_KEYS=
